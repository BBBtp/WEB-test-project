version: '3.7'

services:
  postgres:
    image: postgres:15-alpine
    container_name: wells_postgres
    environment:
      POSTGRES_DB: wells
      POSTGRES_USER: wells_user
      POSTGRES_PASSWORD: 1
      POSTGRES_HOST_AUTH_METHOD: md5
    ports:
      - "5455:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wells_user -d wells"]
      interval: 10s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: minio_wells
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  redis:
    image: redis:7-alpine
    container_name: wells_redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: ["redis-server", "--save", "", "--appendonly", "no"]

  web:
    build: .
    container_name: wells_web
    ports:
      - "8000:8000"
      - "8443:8443"
    environment:
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=wells
      - DB_USER=wells_user
      - DB_PASSWORD=1
      - DB_HOST=postgres
      - DB_PORT=5432
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - SECURE_SSL_REDIRECT=False
      - USE_HTTPS=true
      - ASYNC_SERVICE_URL=https://async_service:8444
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_healthy
      redis:
        condition: service_started
      async_service:
        condition: service_started
    volumes:
      - .:/app
      - ./keys:/app/keys
    command: >
            sh -c "python manage.py makemigrations &&
                   python manage.py migrate &&
                   echo 'from django.contrib.auth import get_user_model; User = get_user_model(); User.objects.filter(username=\"admin\").exists() or User.objects.create_superuser(\"admin\", \"admin@example.com\", \"admin\")' | python manage.py shell &&
                   if [ \"$$USE_HTTPS\" = \"true\" ] || [ \"$$USE_HTTPS\" = \"1\" ]; then
                     echo '=== Запуск в режиме HTTPS ===' &&
                     mkdir -p /app/keys &&
                     if [ ! -f /app/keys/cert.pem ] || [ ! -f /app/keys/key.pem ]; then
                       echo 'SSL сертификаты не найдены. Генерация...' &&
                       python /app/keys/generate_cert.py;
                     else
                       echo 'SSL сертификаты уже существуют.';
                     fi &&
                     echo 'Запуск Gunicorn на порту 8443 (HTTPS)...' &&
                     exec gunicorn --bind 0.0.0.0:8443 --keyfile /app/keys/key.pem --certfile /app/keys/cert.pem --workers 3 --timeout 120 wells_project.wsgi:application;
                   else
                     echo '=== Запуск в режиме HTTP ===' &&
                     echo 'Запуск Gunicorn на порту 8000 (HTTP)...' &&
                     exec gunicorn --bind 0.0.0.0:8000 --workers 3 --timeout 120 wells_project.wsgi:application;
                   fi"

  async_service:
    build:
      context: ./async_service
      dockerfile: Dockerfile
    container_name: wells_async_service
    ports:
      - "8001:8001"
      - "8444:8444"
    environment:
      - DJANGO_SERVICE_URL=http://web:8000
      - DJANGO_USE_HTTPS=true
      - API_TOKEN=secret123
      - ASYNC_SERVICE_PORT=8001
      - ASYNC_SERVICE_HTTPS_PORT=8444
      - USE_HTTPS=true
    volumes:
      - ./async_service:/app
      - ./async_service/keys:/app/keys
    command: >
            sh -c "if [ \"$$USE_HTTPS\" = \"true\" ] || [ \"$$USE_HTTPS\" = \"1\" ]; then
                     echo '=== Запуск async_service в режиме HTTPS ===' &&
                     mkdir -p /app/keys &&
                     if [ ! -f /app/keys/async_cert.pem ] || [ ! -f /app/keys/async_key.pem ]; then
                       echo 'SSL сертификаты не найдены. Генерация...' &&
                       python /app/generate_cert.py;
                     else
                       echo 'SSL сертификаты уже существуют.';
                     fi &&
                     echo 'Запуск uvicorn на порту 8444 (HTTPS)...' &&
                     exec uvicorn main:app --host 0.0.0.0 --port 8444 --ssl-keyfile /app/keys/async_key.pem --ssl-certfile /app/keys/async_cert.pem;
                   else
                     echo '=== Запуск async_service в режиме HTTP ===' &&
                     echo 'Запуск uvicorn на порту 8001 (HTTP)...' &&
                     exec uvicorn main:app --host 0.0.0.0 --port 8001;
                   fi"

volumes:
  postgres_data:
  minio_data:
  redis_data:
